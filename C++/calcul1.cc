#include <iostream>
#include <cmath>
using namespace std;


int main()
{
	int x(2);
	int y(4);
	double a(0);
	double b(0);
	double c(0);
	double d(0);
	a=x+y;
	b=x-y;
	c=x*y;
	d=x/y;
	cout <<"x= "<<x<<" y= "<<y<<" la somme a= "<<a<<" la difference b= "<<b<<" le produit c= "<<c<<" le quotient d= "<<d<<endl;
	return 0;
	
}

